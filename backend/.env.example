# Copy to .env and adjust. Used by the FastAPI backend when connecting to Postgres.
# Example matches docker-compose.yml at repo root.

DATABASE_URL=postgresql+psycopg://rainuse:rainuse@localhost:5432/rainuse
