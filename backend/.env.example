# Copy to backend/.env (or set the same variables in the repo root .env — both are loaded;
# backend/.env overrides the root file when keys conflict).

DATABASE_URL=postgresql+psycopg://rainuse:rainuse@localhost:5432/rainuse

# Required for POST /api/keys/issue — must match collector/.env.local (NEXT_PUBLIC_*)
AUTH0_DOMAIN=dev-d1fhmjclpnx4r03a.us.auth0.com
AUTH0_CLIENT_ID=your-spa-client-id
# AUTH0_AUDIENCE=
